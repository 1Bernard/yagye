<%= render Disputes::IndexPage.new(
  tab:       @tab,
  disputes:  @disputes,
  pagy:      @pagy,
  query:     params[:q],
  reason:    params[:reason],
  date_from: params[:from],
  date_to:   params[:to]
) %>
